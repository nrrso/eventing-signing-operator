\* TLC model configuration for CeSigningReconciler.tla
\*
\* Run with:
\*   tlc CeSigningReconciler.tla -config CeSigningReconciler.cfg -workers auto
\*
\* Or via the TLA+ Toolbox / VS Code TLA+ extension.

CONSTANTS
    Namespaces     = {"alice", "bob"}
    MaxKeyVersion  = 2
    MaxRegistryRetries = 3

INVARIANT
    Safety

PROPERTY
    LabelEventuallyConsumed
    CacheEventuallyConverges

SPECIFICATION
    Spec
